spring.application.name=transcription-pipeline

# File upload limits
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Local storage for uploaded audio (replace with S3 in production)
app.storage.location=./uploads

# Server
server.port=8080
